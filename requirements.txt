torch>=2.4
numpy>=1.26
tqdm>=4.66
pytest>=8.0

# Tokenizer : regex pour le pré-découpage (\p{L}), tiktoken pour encoder vite
regex>=2024.0
tiktoken>=0.8

# Corpus : lecture en streaming de FineWeb-2
datasets>=3.0
